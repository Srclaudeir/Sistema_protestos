# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=protestos_db
DB_PORT=3306

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# JWT Configuration (for future authentication)
JWT_SECRET=protestos_jwt_secret_key
JWT_EXPIRES_IN=24h

# Application Configuration
APP_NAME="Sistema de Protestos Financeiros"
APP_VERSION=1.0.0

# CORS Configuration (use comma to allow multiple origins, e.g. http://localhost:5173,http://10.10.4.94:5173)
CORS_ORIGIN=http://localhost:5173

# Logging Configuration
LOG_LEVEL=info

# Pagination Configuration
DEFAULT_PAGE_SIZE=10
MAX_PAGE_SIZE=100
